services:
  mongodb:
    image: mongo:8
    container_name: cmpdi-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - cmpdi_mongo_data:/data/db
volumes:
  cmpdi_mongo_data:
